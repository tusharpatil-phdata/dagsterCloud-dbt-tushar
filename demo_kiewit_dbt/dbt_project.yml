name: dagsterCloud-dbt-project
version: '1.0.0'
config-version: 2
profile: dagsterCloud-dbt-project

model-paths:    [models]
seed-paths:     [seeds]
test-paths:     [tests]
analysis-paths: [analyses]
macro-paths:    [macros]
snapshot-paths: [snapshots]
target-path: target
clean-targets: [target, dbt_packages]

models:
  dagsterCloud-dbt-project:
    lz:
      +database: DAGSTER_DBT_KIEWIT_DB_CLOUD
      +schema: lz
      +materialized: table
      +tags: [lz, bronze]
      +group: bronze
    staging:
      +database: DAGSTER_DBT_KIEWIT_DB_CLOUD
      +schema: staging
      +materialized: view
      +tags: [staging, silver]
      +group: silver
    dbo:
      +database: DAGSTER_DBT_KIEWIT_DB_CLOUD
      +schema: dbo
      +materialized: table
      +tags: [dbo, gold]
      +group: gold